INSERT INTO ASSETS (
    ID,
    ASSET_CLASS,
    EXCHANGE,
    SYMBOL,
    NAME,
    STATUS,
    TRADABLE,
    MARGINABLE,
    SHORTABLE,
    EASY_TO_BORROW,
    FRACTIONABLE,
    MIN_ORDER_SIZE,
    MIN_TRADE_INCREMENT,
    PRICE_INCREMENT,
    MAINTENANCE_MARGIN_REQUREMENT,
    ATTRIBUTES,
    INSERT_ID,
    UPDATE_ID
)
SELECT
    ID,
    ASSET_CLASS,
    EXCHANGE,
    SYMBOL,
    NAME,
    STATUS,
    TRADABLE,
    MARGINABLE,
    SHORTABLE,
    EASY_TO_BORROW,
    FRACTIONABLE,
    MIN_ORDER_SIZE,
    MIN_TRADE_INCREMENT,
    PRICE_INCREMENT,
    MAINTENANCE_MARGIN_REQUREMENT,
    ATTRIBUTES,
    INSERT_ID,
    UPDATE_ID
FROM TEMP_ASSETS
ON CONFLICT (ID) DO UPDATE
SET
    ASSET_CLASS = EXCLUDED.ASSET_CLASS,
    EXCHANGE = EXCLUDED.EXCHANGE,
    SYMBOL = EXCLUDED.SYMBOL,
    NAME = EXCLUDED.NAME,
    STATUS = EXCLUDED.STATUS,
    TRADABLE = EXCLUDED.TRADABLE,
    MARGINABLE = EXCLUDED.MARGINABLE,
    SHORTABLE = EXCLUDED.SHORTABLE,
    EASY_TO_BORROW = EXCLUDED.EASY_TO_BORROW,
    FRACTIONABLE = EXCLUDED.FRACTIONABLE,
    MIN_ORDER_SIZE = EXCLUDED.MIN_ORDER_SIZE,
    MIN_TRADE_INCREMENT = EXCLUDED.MIN_TRADE_INCREMENT,
    PRICE_INCREMENT = EXCLUDED.PRICE_INCREMENT,
    MAINTENANCE_MARGIN_REQUREMENT = EXCLUDED.MAINTENANCE_MARGIN_REQUREMENT,
    ATTRIBUTES = EXCLUDED.ATTRIBUTES,
    INSERT_ID = EXCLUDED.INSERT_ID,
    UPDATE_ID = EXCLUDED.UPDATE_ID;